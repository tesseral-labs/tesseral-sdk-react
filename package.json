{
  "name": "@tesseral/tesseral-react",
  "version": "0.0.8",
  "description": "",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "fmt": "prettier --write src",
    "build": "rm -rf dist && tsc",
    "dev": "npm-run-all -p dev:*",
    "dev:esbuild": "cd src/test-app && esbuild --bundle --outfile=public/index.js ./src --watch",
    "dev:serve": "cd src/test-app && serve public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tesseral-labs/tesseral-react"
  },
  "devDependencies": {
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4",
    "esbuild": "^0.25.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.5.2",
    "react-dom": "^19.0.0",
    "serve": "^14.2.4",
    "typescript": "^5.7.3"
  },
  "peerDependencies": {
    "react": "^16.8.0 || ^17.0.0 || ^18.0.0 || ^19.0.0"
  },
  "dependencies": {
    "@tesseral/tesseral-vanilla-clientside": "^0.0.5"
  }
}
